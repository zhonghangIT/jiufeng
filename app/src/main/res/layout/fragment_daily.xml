<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ptr="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/background_daily"
    android:orientation="vertical">

    <TextView
        android:id="@+id/textview_null"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="无日志信息" />

    <com.uniquedu.cemetery.pulltorefresh.PullToRefreshListView
        android:id="@+id/daily_list"
        android:layout_width="match_parent"
        android:divider="#00000000"
        android:layout_height="match_parent"
        android:visibility="visible"
        ptr:ptrMode="both"></com.uniquedu.cemetery.pulltorefresh.PullToRefreshListView>

</FrameLayout>